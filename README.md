# Sandbox .NET Core TPL (task Parallel Library)
Sandbox to play with TPL

## Summary
Basically the project runs an example with `Task.WaitAny` and another with `Task.WaitAll`

## Pre-requisites
- .NET Core 2.0